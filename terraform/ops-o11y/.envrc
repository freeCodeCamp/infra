source_env ../..

if [ -d "$SECRETS_DIR" ]; then
  use_sops "$SECRETS_DIR/do-universe/.env.enc"
  use_sops "$SECRETS_DIR/tfstate/.env.enc"
fi

if [ -n "${DO_API_TOKEN:-}" ]; then
  export DIGITALOCEAN_TOKEN="$DO_API_TOKEN"
fi
