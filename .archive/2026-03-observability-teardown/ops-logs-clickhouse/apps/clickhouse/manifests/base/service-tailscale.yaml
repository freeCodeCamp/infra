# ClickHouse service exposed via Tailscale
# Creates a stable hostname: ops-k3s-clickhouse-logs.<tailnet>.ts.net
apiVersion: v1
kind: Service
metadata:
  name: clickhouse-ingress
  namespace: clickhouse
  annotations:
    tailscale.com/hostname: "ops-k3s-clickhouse-logs"
    tailscale.com/tags: "tag:k8s"
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    clickhouse.altinity.com/chi: logs
  ports:
    - name: http
      port: 8123
      targetPort: 8123
      protocol: TCP
    - name: native
      port: 9000
      targetPort: 9000
      protocol: TCP
