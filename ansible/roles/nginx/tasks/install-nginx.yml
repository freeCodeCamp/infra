---
- name: Install NGINX and its dependecies via apt
  apt:
    name: "{{ item }}"
    state: present
  with_items:
    - nginx

- name: Stop the stock NGINX until the config and the upstreams are in place
  ansible.builtin.systemd:
    name: nginx
    state: stopped
