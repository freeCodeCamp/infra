---
- name: Install and configure fail2ban
  hosts: '{{ variable_host | default("null") }}'
  serial: "{{ variable_serial | default(1) }}"
  gather_facts: false
  become: true
  become_user: root
  roles:
    - fail2ban
