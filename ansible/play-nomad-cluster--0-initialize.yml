---
- name: Initialize Publications Cluster
  hosts: '{{ variable_host | default("null") }}'
  become: true
  roles:
    # - ubuntu # Update the OS and reboot the server
    - dns # Configure ansible facts for networking info lookup
    - nomad # Install and configure Nomad
