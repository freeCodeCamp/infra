---
- name: Remove node_exporter from the fleet
  hosts: "{{ variable_host | default('null') }}"
  gather_facts: false
  become: true
  become_user: root
  tasks:
    - name: Remove every node_exporter artefact
      ansible.builtin.include_role:
        name: node-exporter
        tasks_from: remove
