# Sprint 2026-04-21 — Universe Static-Apps MVP

**Goal:** staff team pushes to a repo → site live on gxy-cassiopeia via R2,
served by Caddy, end-to-end, production-polished, ready for real users.

## Resuming work (fresh Claude Code session)

1. Read [`HANDOFF.md`](HANDOFF.md) end-to-end. It has all session context,
   galaxy state, task map, decisions, and key file pointers.
2. Read `docs/GUIDELINES.md` (doc conventions; written in task #17).
3. Run `TaskList` and pick the next unblocked task.
4. Verify live state still matches HANDOFF before acting on assumptions.

## What's here

- `HANDOFF.md` — session resumption doc (primary).
- `MASTER.md` — dispatch checklist (written in task #23; not yet present).
- Per-task dispatch files — added as tasks dispatch.

The prior sprint `../2026-04-20/` is scrapped and will be archived under
`../archive/2026-04-20/` during the docs reorg (task #17).
