---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: secure-headers
  namespace: windmill
spec:
  headers:
    customRequestHeaders:
      X-Forwarded-Proto: "https"

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-https
  namespace: windmill
spec:
  redirectScheme:
    scheme: https
    permanent: true

---
# HTTP to HTTPS redirect
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-redirect
  namespace: windmill
spec:
  parentRefs:
    - name: windmill-gateway
      namespace: windmill
      sectionName: web
  hostnames:
    - windmill.freecodecamp.net
  rules:
    - filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: redirect-https
      backendRefs:
        - name: windmill-app
          port: 8000

---
# Main Windmill route
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: windmill-route
  namespace: windmill
spec:
  parentRefs:
    - name: windmill-gateway
      namespace: windmill
      sectionName: websecure
  hostnames:
    - windmill.freecodecamp.net
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: secure-headers
      backendRefs:
        - name: windmill-app
          port: 8000
