apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: zot-gateway
  namespace: zot
spec:
  gatewayClassName: traefik
  listeners:
    - name: websecure
      protocol: HTTPS
      port: 8443
      hostname: registry.freecodecamp.net
      tls:
        mode: Terminate
        certificateRefs:
          - name: zot-tls-cloudflare
      allowedRoutes:
        namespaces:
          from: Same
    - name: web
      protocol: HTTP
      port: 8000
      hostname: registry.freecodecamp.net
      allowedRoutes:
        namespaces:
          from: Same
