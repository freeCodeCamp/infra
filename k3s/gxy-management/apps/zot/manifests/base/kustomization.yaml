apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: zot

resources:
  - namespace.yaml
  - gateway.yaml
  - httproutes.yaml

secretGenerator:
  - name: zot-secrets
    type: Opaque
    envs:
      - secrets/.secrets.env
    options:
      disableNameSuffixHash: true
