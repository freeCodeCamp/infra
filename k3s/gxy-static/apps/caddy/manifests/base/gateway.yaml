apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: caddy-gateway
  namespace: caddy
spec:
  gatewayClassName: traefik
  listeners:
    - name: web
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
