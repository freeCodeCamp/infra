# Tailscale Kubernetes Operator Helm values
# Install: helm install tailscale-operator tailscale/tailscale-operator \
#   --namespace tailscale --create-namespace -f operator-values.yaml
oauth:
  clientId: ""
  clientSecret: ""
operatorConfig:
  hostname: "ops-k3s-backoffice-operator"
  defaultTags:
    - "tag:k8s"
