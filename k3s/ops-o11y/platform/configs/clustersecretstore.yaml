apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: onepassword
spec:
  provider:
    onepassword:
      connectHost: http://onepassword-connect.external-secrets.svc:8080
      vaults:
        infra: 1
      auth:
        secretRef:
          connectTokenSecretRef:
            name: op-connect-token
            key: token
            namespace: external-secrets
