apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vm-backup-r2
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: vm-backup-r2
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: mgmt-r2-backup
