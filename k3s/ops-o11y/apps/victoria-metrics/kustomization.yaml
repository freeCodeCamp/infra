apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: o11y
resources:
  - helmrepository.yaml
  - externalsecret.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: victoria-metrics-values
    files:
      - values.yaml=charts/victoria-metrics-single/values.yaml
      - values-mgmt.yaml
configurations:
  - kustomizeconfig.yaml
